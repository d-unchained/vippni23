specVersion: "v1beta"
serviceId: "vippni"
location: "australia-southeast2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vippni-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
